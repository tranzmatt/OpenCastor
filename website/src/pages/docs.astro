---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Documentation — OpenCastor"
  description="Get started with OpenCastor in 5 minutes. Setup v2 includes Apple Foundation Models, stack-aware preflight checks, guided fallbacks, and full API/CLI docs."
  ogUrl="https://opencastor.com/docs"
>

<main>
    <section class="page-hero">
      <p class="section-label reveal">Documentation</p>
      <h1 class="reveal reveal-d1">From zero to robot<br>in five minutes.</h1>
      <p class="reveal reveal-d2">Everything you need to install, configure, and deploy your first AI-powered robot.
        v2026.3.8.1</p>
    </section>

    <!-- Table of Contents -->
    <section class="section" style="padding-top:0;padding-bottom:20px">
      <div class="container">
        <div class="toc reveal">
          <h3>📖 Table of Contents</h3>
          <div class="toc-cols">
            <div>
              <a href="#install">Quick Start</a>
              <a href="#wizard">Setup Wizard</a>
              <a href="#configuration">Configuration (RCAN)</a>
              <a href="#providers">AI Providers Guide</a>
              <a href="#tiered-brain">Tiered Brain Architecture</a>
              <a href="#peripherals">Peripherals &amp; Auto-Detect</a>
            </div>
            <div>
              <a href="#hailo">Hailo-8 (example NPU)</a>
              <a href="#safety">Safety Modules</a>
              <a href="#cli">CLI Reference</a>
              <a href="#api">API Reference</a>
              <a href="#skill-invocation">Skill Invocation</a>
              <a href="#docker">Docker</a>
              <a href="#troubleshooting">Troubleshooting</a>
              <a href="/hub">Community Recipes</a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Installation / Quick Start -->
    <section class="section" id="install" style="padding-top:20px">
      <div class="container">
        <div class="quickstart reveal">
          <h2 style="font-size:1.5rem;margin-bottom:28px">Quick Start</h2>

          <div class="quickstart-step">
            <div class="quickstart-num">1</div>
            <div>
              <h4>Install OpenCastor</h4>
              <p style="font-size:0.9375rem;color:var(--text-secondary);margin-bottom:8px">One command installs
                everything: Python deps, hardware drivers, and the interactive wizard.</p>
              <div class="code-block" style="margin-bottom:12px">
                <div class="code-header">
                  <div class="code-dots"><span></span><span></span><span></span></div><span class="code-filename">Linux
                    / macOS</span>
                </div>
                <div class="code-body">curl -sL opencastor.com/install | bash</div>
              </div>
              <div class="code-block" style="margin-bottom:12px">
                <div class="code-header">
                  <div class="code-dots"><span></span><span></span><span></span></div><span class="code-filename">pip
                    (all platforms)</span>
                </div>
                <div class="code-body">pip install opencastor</div>
              </div>
              <div class="code-block" style="margin-bottom:12px">
                <div class="code-header">
                  <div class="code-dots"><span></span><span></span><span></span></div><span
                    class="code-filename">Windows (PowerShell)</span>
                </div>
                <div class="code-body">irm opencastor.com/install.ps1 | iex</div>
              </div>
              <p style="font-size:0.875rem;color:var(--text-secondary);margin-top:8px">Optional on macOS: preinstall
                Apple SDK support with <code style="padding:2px 5px;font-size:0.8125rem">pip install "git+https://github.com/apple/python-apple-fm-sdk.git@3204b7ee892131a5d2c940d95caaabc90b4a40c9"</code>
                or install via <code style="padding:2px 5px;font-size:0.8125rem">curl -sL opencastor.com/install | bash -s -- --with-apple-sdk</code>.
                Setup can also install it later with your consent.</p>
            </div>
          </div>

          <div class="quickstart-step">
            <div class="quickstart-num">2</div>
            <div>
              <h4>Run the setup wizard</h4>
              <p style="font-size:0.9375rem;color:var(--text-secondary);margin-bottom:8px">Setup v2 walks you through
                device-aware stack selection, model profile presets, preflight checks, fallbacks, and channels.</p>
              <code>castor wizard</code>
            </div>
          </div>

          <div class="quickstart-step">
            <div class="quickstart-num">3</div>
            <div>
              <h4>Launch</h4>
              <p style="font-size:0.9375rem;color:var(--text-secondary);margin-bottom:8px">Start the runtime. Your robot
                is now an AI agent accepting commands via WhatsApp, voice, or API.</p>
              <code>castor run --config my_robot.rcan.yaml</code>
            </div>
          </div>

          <div class="quickstart-step">
            <div class="quickstart-num">4</div>
            <div>
              <h4>Verify</h4>
              <p style="font-size:0.9375rem;color:var(--text-secondary);margin-bottom:8px">Run the doctor to check that
                all dependencies, drivers, and providers are correctly configured.</p>
              <code>castor doctor</code>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Wizard Walkthrough -->
    <section class="section" id="wizard">
      <div class="container">
        <div class="section-header reveal" style="text-align:center;margin-left:auto;margin-right:auto">
          <p class="section-label">Setup Wizard</p>
          <h2 class="section-title">What to expect when<br>you run <code
              style="font-family:var(--font-mono);color:var(--accent)">castor wizard</code></h2>
          <p class="section-desc" style="margin-left:auto;margin-right:auto">CLI and web now use the same setup-v2
            flow: device-aware stack selection, profile presets, preflight checks, and guided fallbacks.</p>
        </div>

        <div class="wizard-step reveal">
          <div class="wizard-step-header">
            <div class="wizard-step-num">1</div>
            <div>
              <h3>Name your project + review device probe</h3>
              <p>Setup detects your OS/architecture first so only compatible stacks are offered.</p>
            </div>
          </div>
          <div class="code-block">
            <div class="code-header">
              <div class="code-dots"><span></span><span></span><span></span></div><span
                class="code-filename">Terminal</span>
            </div>
            <div class="code-body"><span class="cm">--- PROJECT ---</span>
              <span class="cm">Project Name [MyRobot]:</span> <span class="s">Explorer</span>
              <span class="v">[OK]</span> <span class="cm">Detected platform: macOS arm64</span>
              <span class="v">[OK]</span> <span class="cm">Apple Intelligence eligibility check enabled</span>
            </div>
          </div>
        </div>

        <div class="wizard-step reveal">
          <div class="wizard-step-header">
            <div class="wizard-step-num">2</div>
            <div>
              <h3>Choose a stack profile</h3>
              <p>Pick a curated stack first; setup maps it to provider + model defaults.</p>
            </div>
          </div>
          <div class="code-block">
            <div class="code-header">
              <div class="code-dots"><span></span><span></span><span></span></div><span
                class="code-filename">Terminal</span>
            </div>
            <div class="code-body"><span class="cm">--- STACK PROFILE ---</span>
              <span class="cm">Select a software stack for this device:</span>
              <span class="k">[1]</span> <span class="s">apple_native</span> <span class="cm">-- Apple Foundation Models on eligible Macs</span>
              <span class="k">[2]</span> <span class="s">mlx_local_vision</span> <span class="cm">-- Apple Silicon local fallback</span>
              <span class="k">[3]</span> <span class="s">ollama_universal_local</span> <span class="cm">-- cross-platform local default</span>
              <span class="cm">Selection [1]:</span> <span class="s">1</span>
            </div>
          </div>
        </div>

        <div class="wizard-step reveal">
          <div class="wizard-step-header">
            <div class="wizard-step-num">3</div>
            <div>
              <h3>Select a model profile</h3>
              <p>Apple profiles are preset-based so users do not need to tune raw guardrail enums.</p>
            </div>
          </div>
          <div class="code-block">
            <div class="code-header">
              <div class="code-dots"><span></span><span></span><span></span></div><span class="code-filename">Terminal — Apple profiles</span>
            </div>
            <div class="code-body"><span class="cm">--- MODEL PROFILE ---</span>
              <span class="k">[1]</span> <span class="s">apple-balanced</span> <span class="v">(Recommended)</span>
              <span class="k">[2]</span> <span class="s">apple-creative</span>
              <span class="k">[3]</span> <span class="s">apple-tagging</span>
              <span class="cm">Selection [1]:</span> <span class="s">1</span>
            </div>
          </div>
        </div>

        <div class="wizard-step reveal">
          <div class="wizard-step-header">
            <div class="wizard-step-num">4</div>
            <div>
              <h3>Run preflight + remediation</h3>
              <p>Setup validates SDK/runtime prerequisites and can auto-install Apple SDK with explicit consent.</p>
            </div>
          </div>
          <div class="code-block">
            <div class="code-header">
              <div class="code-dots"><span></span><span></span><span></span></div><span class="code-filename">Terminal — Preflight</span>
            </div>
            <div class="code-body"><span class="cm">--- PREFLIGHT ---</span>
              <span class="v">[WARN]</span> <span class="cm">apple_fm_sdk_import: missing</span>
              <span class="cm">Install Apple SDK now? [Y/n]:</span> <span class="s">Y</span>
              <span class="v">[OK]</span> <span class="cm">Re-running preflight...</span>
              <span class="v">[OK]</span> <span class="cm">Apple provider ready</span>
            </div>
          </div>
        </div>

        <div class="wizard-step reveal">
          <div class="wizard-step-header">
            <div class="wizard-step-num">5</div>
            <div>
              <h3>Fallback chooser <span
                  style="font-weight:400;font-size:0.875rem;color:var(--text-secondary)">(when needed)</span></h3>
              <p>If Apple is unavailable, setup recommends local-first compatible stacks before cloud options.</p>
            </div>
          </div>
        </div>

        <div class="wizard-step reveal">
          <div class="wizard-step-header">
            <div class="wizard-step-num">6</div>
            <div>
              <h3>Select hardware preset + channels</h3>
              <p>QuickStart now includes explicit hardware preset selection, then optional WhatsApp/Telegram setup.</p>
            </div>
          </div>
        </div>

        <div class="wizard-step reveal">
          <div class="wizard-step-header">
            <div class="wizard-step-num" style="background:var(--green-light);color:var(--green)">&#10003;</div>
            <div>
              <h3>Config generated</h3>
              <p>The wizard outputs a validated <code style="padding:2px 6px;font-size:0.8125rem">.rcan.yaml</code>
                config file ready to run.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Configuration -->
    <section class="section alt-section" id="configuration">
      <div class="container">
        <div class="section-header center reveal">
          <p class="section-label">Configuration</p>
          <h2 class="section-title">RCAN YAML Format</h2>
          <p class="section-desc">Every OpenCastor robot is defined by a single <code
              style="padding:2px 6px;font-size:0.8125rem">.rcan.yaml</code> config file following the RCAN
            specification.</p>
        </div>

        <div class="code-block reveal" style="max-width:720px;margin:0 auto 32px">
          <div class="code-header">
            <div class="code-dots"><span></span><span></span><span></span></div><span
              class="code-filename">my_robot.rcan.yaml</span>
          </div>
          <div class="code-body"><span class="k">rcan_version: "1.4"</span>

            <span class="k">metadata</span>:
            <span class="k">robot_name</span>: <span class="s">"Explorer"</span>
            <span class="k">robot_uuid</span>: <span class="s">"..."</span> <span class="cm"># auto-generated by
              wizard</span>

            <span class="cm"># --- Agent Block ---</span>
            <span class="k">agent</span>:
            <span class="k">provider</span>: <span class="s">"anthropic"</span> <span class="cm"># anthropic | google |
              openai | huggingface | ollama | llamacpp | mlx | apple | claude_oauth</span>
            <span class="k">model</span>: <span class="s">"claude-opus-4-6"</span>
            <span class="cm"># if provider=apple, model/profile is apple-balanced|apple-creative|apple-tagging</span>
            <span class="k">vision_enabled</span>: <span class="v">true</span>
            <span class="k">safety_stop</span>: <span class="v">true</span>

            <span class="cm"># --- Tiered Brain Block ---</span>
            <span class="k">tiered_brain</span>:
            <span class="k">planner</span>:
            <span class="k">provider</span>: <span class="s">"anthropic"</span>
            <span class="k">model</span>: <span class="s">"claude-opus-4-6"</span>
            <span class="k">max_latency_ms</span>: <span class="n">5000</span>
            <span class="k">fast_brain</span>:
            <span class="k">provider</span>: <span class="s">"huggingface"</span>
            <span class="k">model</span>: <span class="s">"meta-llama/Llama-3.3-70B-Instruct"</span>
            <span class="k">max_latency_ms</span>: <span class="n">500</span>
            <span class="k">escalation_threshold</span>: <span class="n">0.7</span> <span class="cm"># confidence below
              this → escalate to planner</span>

            <span class="cm"># --- Reactive Block ---</span>
            <span class="k">reactive</span>:
            <span class="k">e_stop</span>: <span class="v">true</span>
            <span class="k">collision_avoidance</span>: <span class="v">true</span>
            <span class="k">min_obstacle_distance_cm</span>: <span class="n">15</span>
            <span class="k">max_velocity</span>: <span class="n">1.5</span> <span class="cm"># m/s</span>

            <span class="cm"># --- Camera Block ---</span>
            <span class="k">camera</span>:
            <span class="k">type</span>: <span class="s">"oakd"</span> <span class="cm"># oakd | csi | usb |
              hailo</span>
            <span class="k">depth_enabled</span>: <span class="v">true</span>
            <span class="k">resolution</span>: <span class="s">"640x480"</span>
            <span class="k">fps</span>: <span class="n">30</span>

            <span class="k">physics</span>:
            <span class="k">type</span>: <span class="s">"differential_drive"</span>
            <span class="k">dof</span>: <span class="n">2</span>

            <span class="k">drivers</span>:
            - <span class="k">protocol</span>: <span class="s">"pca9685_i2c"</span>
            <span class="k">address</span>: <span class="s">"0x40"</span>

            <span class="k">rcan_protocol</span>:
            <span class="k">port</span>: <span class="n">8000</span>
            <span class="k">capabilities</span>: <span class="s">[status, nav, teleop, vision, chat]</span>
          </div>
        </div>

        <div class="doc-section reveal">
          <h3>Key Blocks</h3>
          <ul>
            <li><strong>agent</strong> — Primary AI provider, model, and feature flags.</li>
            <li><strong>tiered_brain</strong> — Multi-tier architecture: planner (slow/smart), fast_brain (fast/cheap),
              and escalation rules.</li>
            <li><strong>reactive</strong> — Hardware-level safety: e-stop, collision avoidance, velocity limits. Runs
              below AI.</li>
            <li><strong>camera</strong> — Camera configuration: type (oakd, csi, usb, hailo), depth, resolution, FPS.
            </li>
            <li><strong>physics</strong> — Robot kinematics: differential_drive, ackermann, holonomic, arm.</li>
            <li><strong>drivers</strong> — Hardware drivers: PCA9685, Dynamixel, GPIO, serial.</li>
            <li><strong>rcan_protocol</strong> — Network port and declared RCAN capabilities.</li>
          </ul>
        </div>
      </div>
    </section>

    <!-- Providers Guide -->
    <section class="section" id="providers">
      <div class="container">
        <div class="section-header center reveal">
          <p class="section-label">Providers</p>
          <h2 class="section-title">Apple Foundation Models + 12+ providers.</h2>
          <p class="section-desc">Setup picks compatible stacks first (<code style="padding:2px 5px;font-size:0.8125rem">apple_native</code>,
            <code style="padding:2px 5px;font-size:0.8125rem">mlx_local_vision</code>,
            <code style="padding:2px 5px;font-size:0.8125rem">ollama_universal_local</code>), then lets you swap providers with one config change.</p>
        </div>

        <!-- SETUP_CATALOG:BEGIN -->
<div class="wizard-tip reveal" style="margin-bottom:20px">
          <strong>Catalog snapshot:</strong> stacks: <code style="padding:2px 5px;font-size:0.8125rem">apple_native</code>,
          <code style="padding:2px 5px;font-size:0.8125rem">mlx_local_vision</code>,
          <code style="padding:2px 5px;font-size:0.8125rem">ollama_universal_local</code> · Apple profiles:
          <code style="padding:2px 5px;font-size:0.8125rem">apple-balanced</code>,
          <code style="padding:2px 5px;font-size:0.8125rem">apple-creative</code>,
          <code style="padding:2px 5px;font-size:0.8125rem">apple-tagging</code>
        </div>
<!-- SETUP_CATALOG:END -->

        <div class="guide-grid">
          <div class="guide-card reveal">
            <h3><span style="color:#111">●</span> Apple Foundation Models</h3>
            <p>On-device Apple Intelligence language model for eligible Macs. Profile presets:
              <code style="padding:2px 5px;font-size:0.8125rem">apple-balanced</code>,
              <code style="padding:2px 5px;font-size:0.8125rem">apple-creative</code>,
              <code style="padding:2px 5px;font-size:0.8125rem">apple-tagging</code>. No API key required.</p>
            <div class="hw-card-meta" style="margin-top:12px">provider: "apple" · no auth required · preflight checks</div>
          </div>
          <div class="guide-card reveal">
            <h3><span style="color:#D97706">●</span> Anthropic</h3>
            <p>Claude Opus 4, Sonnet 4, Haiku 3.5. Best for complex planning and safety-critical reasoning. API key or
              OAuth.</p>
            <div class="hw-card-meta" style="margin-top:12px">provider: "anthropic" · ANTHROPIC_API_KEY</div>
          </div>
          <div class="guide-card reveal reveal-d1">
            <h3><span style="color:#4285F4">●</span> Google Gemini</h3>
            <p>Gemini 2.5 Flash, Pro, and Robotics ER. Excellent vision and embodied reasoning. Google AI Studio key.
            </p>
            <div class="hw-card-meta" style="margin-top:12px">provider: "google" · GOOGLE_API_KEY</div>
          </div>
          <div class="guide-card reveal reveal-d2">
            <h3><span style="color:#10A37F">●</span> OpenAI</h3>
            <p>GPT-4.1, GPT-4o, o3. Strong tool use and function calling. Traditional API key.</p>
            <div class="hw-card-meta" style="margin-top:12px">provider: "openai" · OPENAI_API_KEY</div>
          </div>
          <div class="guide-card reveal reveal-d3">
            <h3><span style="color:#FFD21E">●</span> Hugging Face</h3>
            <p>1M+ models including Llama 3.3, Mistral, Qwen. Free inference API tier. Best for $0 fast brain.</p>
            <div class="hw-card-meta" style="margin-top:12px">provider: "huggingface" · HF_TOKEN</div>
          </div>
          <div class="guide-card reveal reveal-d4">
            <h3><span style="color:#888">●</span> Ollama</h3>
            <p>Run models locally with zero cloud dependency. Llama 3.3, Mistral, Phi-3. No API key needed.</p>
            <div class="hw-card-meta" style="margin-top:12px">provider: "ollama" · no auth required</div>
          </div>
          <div class="guide-card reveal reveal-d5">
            <h3><span style="color:#aaa">●</span> llama.cpp</h3>
            <p>Bare-metal local inference with GGUF models. Lowest latency, smallest footprint. Runs on Pi.</p>
            <div class="hw-card-meta" style="margin-top:12px">provider: "llamacpp" · no auth required</div>
          </div>
          <div class="guide-card reveal">
            <h3><span style="color:#7C3AED">●</span> MLX (Apple Silicon)</h3>
            <p>Native GPU inference on Apple Silicon via mlx-lm or vLLM-MLX. Qwen2.5-VL, Llama 3.3, Mistral. 400+ tok/s
              on M4 Max. No API key needed.</p>
            <div class="hw-card-meta" style="margin-top:12px">provider: "mlx" · no auth required · pip install mlx-lm
            </div>
          </div>
          <div class="hw-card">
            <h3><span style="color:#D97706">●</span> Claude OAuth</h3>
            <p>Use your existing Claude Pro/Team subscription — no separate API key. Browser-based OAuth flow.</p>
            <div class="hw-card-meta" style="margin-top:12px">provider: "claude_oauth" · castor login</div>
          </div>
        </div>
      </div>
    </section>

    <!-- Tiered Brain Architecture -->
    <section class="section alt-section" id="tiered-brain">
      <div class="container">
        <div class="section-header center reveal">
          <p class="section-label">Architecture</p>
          <h2 class="section-title">Tiered Cognitive Architecture</h2>
          <p class="section-desc">Three layers of intelligence — from reflexive safety to long-horizon planning. Each
            tier runs independently with automatic escalation.</p>
        </div>

        <div class="arch-visual reveal reveal-d1">
          <div class="arch-tier" style="background: linear-gradient(135deg, rgba(168,85,247,0.08), var(--bg-card));">
            <div class="arch-tier-icon" style="background: var(--purple-light);">🧠</div>
            <div class="arch-tier-content">
              <h3>Planner</h3>
              <p>Long-horizon reasoning, task decomposition, and strategic planning. Claude, GPT-4.1, Gemini. Activated
                when fast brain confidence drops below <code
                  style="font-size:0.75rem;padding:2px 5px">escalation_threshold</code>.</p>
            </div>
            <span class="arch-tier-badge" style="background: var(--purple-light); color: var(--purple);">~2s
              latency</span>
          </div>
          <div class="arch-tier" style="background: linear-gradient(135deg, rgba(99,102,241,0.08), var(--bg-card));">
            <div class="arch-tier-icon" style="background: var(--accent-glow);">⚡</div>
            <div class="arch-tier-content">
              <h3>Fast Brain</h3>
              <p>Real-time perception and quick decisions. Open-source models via HuggingFace, Ollama, or llama.cpp.
                Handles 90%+ of routine tasks at ~100ms.</p>
            </div>
            <span class="arch-tier-badge" style="background: var(--accent-glow); color: var(--accent);">~100ms</span>
          </div>
          <div class="arch-tier" style="background: linear-gradient(135deg, rgba(34,197,94,0.08), var(--bg-card));">
            <div class="arch-tier-icon" style="background: var(--green-light);">🛡️</div>
            <div class="arch-tier-content">
              <h3>Reactive Layer</h3>
              <p>Hardware-level safety reflexes — emergency stop, collision avoidance, physical bounds. No AI latency —
                pure code. Always active, cannot be overridden by model output.</p>
            </div>
            <span class="arch-tier-badge" style="background: var(--green-light); color: var(--green);">&lt;1ms</span>
          </div>
        </div>

        <div class="doc-section reveal" style="margin-top:40px">
          <h3>How Escalation Works</h3>
          <p>The fast brain handles every incoming request first. If its confidence score falls below the configured
            <code style="padding:2px 5px;font-size:0.8125rem">escalation_threshold</code> (default 0.7), the request is
            automatically routed to the planner for deeper reasoning. The reactive layer runs continuously in parallel —
            it intercepts and overrides any command that violates physical safety constraints, regardless of which brain
            tier issued it.</p>
        </div>
      </div>
    </section>

    <!-- Peripherals — Universal plug-and-play -->
    <section class="section" id="peripherals">
      <div class="container">
        <div class="section-header center reveal">
          <p class="section-label">Hardware</p>
          <h2 class="section-title">Plug-and-Play Peripherals</h2>
          <p class="section-desc">Any USB camera, depth sensor, LiDAR, IMU, or motor controller. OpenCastor auto-detects
            at startup — no driver code required.</p>
        </div>

        <div class="doc-section reveal">
          <h3>How auto-detection works</h3>
          <p>At startup (or when you run <code>castor scan</code>), OpenCastor scans:</p>
          <ul>
            <li><strong>USB devices</strong> via <code>lsusb</code> — matches against a library of known VID:PID pairs
              (OAK-D, Intel RealSense, RPLiDAR, common IMUs, motor controllers)</li>
            <li><strong>Video devices</strong> via <code>/dev/video*</code> — any V4L2-compatible camera is usable
              immediately</li>
            <li><strong>I²C bus</strong> via <code>i2cdetect</code> — finds servo controllers (PCA9685), IMUs (MPU-6050,
              BNO055), distance sensors (VL53L0X), and motor drivers</li>
            <li><strong>Serial ports</strong> via <code>/dev/ttyUSB*</code> and <code>/dev/ttyACM*</code> — Arduino,
              motor controllers, GPS, LiDAR</li>
            <li><strong>NPU accelerators</strong> — Hailo-8 via HailoRT, Google Coral Edge TPU, NVIDIA Jetson (via nvml)
            </li>
          </ul>

          <div class="code-block" style="margin:16px 0">
            <div class="code-header">
              <div class="code-dots"><span></span><span></span><span></span></div><span class="code-filename">Scan
                connected hardware</span>
            </div>
            <div class="code-body"><span class="k">castor scan</span>

              <span class="cm"># Example output:</span>
              <span class="s">✓ OAK-D Lite USB 03e7:2485 → camera.type: "oakd"</span>
              <span class="s">✓ Logitech C920 USB 046d:082d → camera.type: "usb" /dev/video0</span>
              <span class="s">✓ RPLiDAR A1 Serial /dev/ttyUSB0 → lidar.type: "rplidar"</span>
              <span class="s">✓ PCA9685 I²C 0x40 → driver.type: "pca9685"</span>
              <span class="s">✓ Hailo-8 NPU PCIe — → camera.npu: "hailo"</span>
              <span class="s"> MPU-6050 I²C 0x68 → imu.type: "mpu6050"</span>
            </div>
          </div>

          <h3>Auto-detect in your RCAN config</h3>
          <p>Set <code>type: "auto"</code> on any peripheral block. OpenCastor will pick the best available hardware at
            runtime — and fall back gracefully if nothing is found.</p>

          <div class="code-block" style="margin:16px 0">
            <div class="code-header">
              <div class="code-dots"><span></span><span></span><span></span></div><span class="code-filename">rcan.yaml
                — auto-detect everything</span>
            </div>
            <div class="code-body"><span class="k">camera</span>:
              <span class="k">type</span>: <span class="s">"auto"</span> <span class="cm"># tries: oakd → realsense →
                hailo → usb → csi → none</span>
              <span class="k">depth_enabled</span>: <span class="v">true</span> <span class="cm"># ignored if detected
                camera has no depth</span>
              <span class="k">fps</span>: <span class="n">30</span>

              <span class="k">lidar</span>:
              <span class="k">type</span>: <span class="s">"auto"</span> <span class="cm"># tries: rplidar → hokuyo →
                sick → none</span>

              <span class="k">imu</span>:
              <span class="k">type</span>: <span class="s">"auto"</span> <span class="cm"># tries: bno055 → mpu6050 →
                icm42688 → none</span>
            </div>
          </div>

          <h3>Supported peripheral categories</h3>
          <table style="width:100%;border-collapse:collapse;font-size:0.9rem">
            <thead>
              <tr style="border-bottom:1px solid #e5e7eb">
                <th style="text-align:left;padding:8px 4px">Category</th>
                <th style="text-align:left;padding:8px 4px">Examples</th>
                <th style="text-align:left;padding:8px 4px">Interface</th>
                <th style="text-align:left;padding:8px 4px">RCAN type</th>
              </tr>
            </thead>
            <tbody>
              <tr style="border-bottom:1px solid #f3f4f6">
                <td style="padding:8px 4px">RGB Camera</td>
                <td style="padding:8px 4px">Any USB webcam, Logitech C920, Pi camera v3</td>
                <td style="padding:8px 4px">USB / CSI</td>
                <td style="padding:8px 4px"><code>camera.type: "usb"</code> / <code>"csi"</code></td>
              </tr>
              <tr style="border-bottom:1px solid #f3f4f6">
                <td style="padding:8px 4px">Depth Camera</td>
                <td style="padding:8px 4px">OAK-D, OAK-D Lite, Intel RealSense D435</td>
                <td style="padding:8px 4px">USB 3</td>
                <td style="padding:8px 4px"><code>camera.type: "oakd"</code> / <code>"realsense"</code></td>
              </tr>
              <tr style="border-bottom:1px solid #f3f4f6">
                <td style="padding:8px 4px">NPU Accelerator</td>
                <td style="padding:8px 4px">Hailo-8, Hailo-8L, Google Coral Edge TPU</td>
                <td style="padding:8px 4px">M.2 / USB</td>
                <td style="padding:8px 4px"><code>camera.npu: "hailo"</code> / <code>"coral"</code></td>
              </tr>
              <tr style="border-bottom:1px solid #f3f4f6">
                <td style="padding:8px 4px">LiDAR</td>
                <td style="padding:8px 4px">RPLiDAR A1/A2/C1, Hokuyo URG, SICK TiM</td>
                <td style="padding:8px 4px">Serial / USB</td>
                <td style="padding:8px 4px"><code>lidar.type: "rplidar"</code></td>
              </tr>
              <tr style="border-bottom:1px solid #f3f4f6">
                <td style="padding:8px 4px">IMU</td>
                <td style="padding:8px 4px">MPU-6050, BNO055, ICM-42688</td>
                <td style="padding:8px 4px">I²C / SPI</td>
                <td style="padding:8px 4px"><code>imu.type: "auto"</code></td>
              </tr>
              <tr style="border-bottom:1px solid #f3f4f6">
                <td style="padding:8px 4px">Servo Controller</td>
                <td style="padding:8px 4px">PCA9685 (16-ch PWM)</td>
                <td style="padding:8px 4px">I²C</td>
                <td style="padding:8px 4px"><code>driver.type: "pca9685"</code></td>
              </tr>
              <tr style="border-bottom:1px solid #f3f4f6">
                <td style="padding:8px 4px">Motor Driver</td>
                <td style="padding:8px 4px">L298N, DRV8833, TB6612, Cytron MD10C</td>
                <td style="padding:8px 4px">GPIO / Serial</td>
                <td style="padding:8px 4px"><code>driver.type: "l298n"</code></td>
              </tr>
              <tr>
                <td style="padding:8px 4px">Microcontroller</td>
                <td style="padding:8px 4px">Arduino Uno/Nano/Mega, STM32, ESP32</td>
                <td style="padding:8px 4px">USB CDC</td>
                <td style="padding:8px 4px"><code>driver.type: "arduino"</code></td>
              </tr>
            </tbody>
          </table>

          <p style="margin-top:16px">Don't see yours? Any device reachable via <code>/dev/video*</code>,
            <code>/dev/ttyUSB*</code>, I²C, or SPI works — just set <code>type: "generic"</code> with a
            <code>device_path</code>. See <a
              href="https://github.com/craigm26/OpenCastor/blob/main/docs/peripherals.md">docs/peripherals.md</a> for
            full details.</p>
        </div>
      </div>
    </section>

    <!-- Hailo-8 Vision — example NPU accelerator -->
    <section class="section" id="hailo">
      <div class="container">
        <div class="section-header center reveal">
          <p class="section-label">Example: NPU Accelerator</p>
          <h2 class="section-title">Hailo-8 NPU Vision</h2>
          <p class="section-desc">One example of supported NPU acceleration. Also works with Google Coral Edge TPU and
            NVIDIA Jetson. No GPU required.</p>
        </div>

        <div class="doc-section reveal">
          <h3>Setup</h3>
          <p>The Hailo-8 M.2 or Hailo-8L module plugs into the Raspberry Pi 5 AI Kit slot or any M.2 host. OpenCastor
            auto-detects the Hailo runtime and loads optimized YOLOv8 models.</p>

          <div class="code-block" style="margin:16px 0">
            <div class="code-header">
              <div class="code-dots"><span></span><span></span><span></span></div><span class="code-filename">Install
                Hailo runtime</span>
            </div>
            <div class="code-body"><span class="cm"># Install Hailo RT (Pi 5 with AI Kit)</span>
              <span class="k">sudo apt install</span> hailo-all
              <span class="k">castor doctor</span> --check hailo
            </div>
          </div>

          <h3>COCO Classes</h3>
          <p>The default YOLOv8 model detects 80 COCO classes out of the box: person, car, bicycle, dog, cat, bottle,
            chair, and 73 more. Custom models can be loaded via the <code
              style="padding:2px 5px;font-size:0.8125rem">hailo_model_path</code> config key.</p>

          <h3>Obstacle Detection Config</h3>
          <div class="code-block" style="margin:16px 0">
            <div class="code-header">
              <div class="code-dots"><span></span><span></span><span></span></div><span class="code-filename">rcan.yaml
                — Hailo vision block</span>
            </div>
            <div class="code-body"><span class="k">camera</span>:
              <span class="k">type</span>: <span class="s">"hailo"</span>
              <span class="k">hailo_model</span>: <span class="s">"yolov8s"</span> <span class="cm"># yolov8n, yolov8s,
                yolov8m</span>
              <span class="k">confidence_threshold</span>: <span class="n">0.5</span>
              <span class="k">obstacle_classes</span>: <span class="s">[person, dog, cat, chair, car]</span>
              <span class="k">obstacle_action</span>: <span class="s">"stop"</span> <span class="cm"># stop | slow |
                avoid</span>
              <span class="k">fps</span>: <span class="n">30</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- OAK-D Camera -->
    <section class="section alt-section" id="oakd">
      <div class="container">
        <div class="section-header center reveal">
          <p class="section-label">Example: Depth &amp; Stereo Camera</p>
          <h2 class="section-title">OAK-D / Depth Cameras</h2>
          <p class="section-desc">OAK-D is one example. Also works with Intel RealSense, ZED, and any USB depth camera.
            Set <code>camera.type: "auto"</code> to detect whichever you have.</p>
        </div>

        <div class="doc-section reveal">
          <p>The OAK-D and OAK-D Lite cameras provide stereo depth maps and on-device ML inference. OpenCastor uses
            DepthAI v3 for low-latency spatial awareness and obstacle mapping.</p>

          <div class="code-block" style="margin:16px 0">
            <div class="code-header">
              <div class="code-dots"><span></span><span></span><span></span></div><span class="code-filename">rcan.yaml
                — OAK-D camera block</span>
            </div>
            <div class="code-body"><span class="k">camera</span>:
              <span class="k">type</span>: <span class="s">"oakd"</span>
              <span class="k">depth_enabled</span>: <span class="v">true</span>
              <span class="k">resolution</span>: <span class="s">"640x480"</span>
              <span class="k">fps</span>: <span class="n">30</span>
              <span class="k">spatial_detection</span>: <span class="v">true</span> <span class="cm"># 3D object
                coordinates</span>
              <span class="k">min_depth_cm</span>: <span class="n">20</span>
              <span class="k">max_depth_cm</span>: <span class="n">500</span>
            </div>
          </div>

          <h3>Features</h3>
          <ul>
            <li>Stereo depth maps at up to 30 FPS</li>
            <li>On-device neural inference (MobileNet, YOLO) via OAK VPU</li>
            <li>Spatial object detection — returns X/Y/Z coordinates in centimeters</li>
            <li>Compatible with OAK-D, OAK-D Lite, OAK-D Pro</li>
            <li>DepthAI v3 pipeline with automatic device discovery</li>
          </ul>
        </div>
      </div>
    </section>

    <!-- Safety Modules -->
    <section class="section" id="safety">
      <div class="container">
        <div class="section-header reveal" style="text-align:center;margin-left:auto;margin-right:auto">
          <p class="section-label">Safety</p>
          <h2 class="section-title">Built-in safety kernel</h2>
          <p class="section-desc" style="margin-left:auto;margin-right:auto">Multi-layered safety enforced at the
            runtime level. Cannot be overridden by model output.</p>
        </div>

        <div class="guide-grid">
          <div class="guide-card reveal">
            <h3>🚨 Emergency Stop</h3>
            <p>Hardware-level e-stop in every control loop. Triggers on collision proximity, timeout, or manual signal.
              Sub-millisecond response.</p>
          </div>
          <div class="guide-card reveal reveal-d1">
            <h3>🛡️ Anti-Subversion</h3>
            <p>Detects and blocks prompt injection, jailbreak attempts, and any AI output that tries to bypass safety
              constraints.</p>
          </div>
          <div class="guide-card reveal reveal-d2">
            <h3>✅ Work Authorization</h3>
            <p>Actions require explicit authorization. The robot cannot perform tasks outside its declared capabilities
              without human approval.</p>
          </div>
          <div class="guide-card reveal reveal-d3">
            <h3>📐 Physical Bounds</h3>
            <p>Enforces velocity limits, workspace boundaries, and collision zones. Hard-coded limits that AI cannot
              modify.</p>
          </div>
          <div class="guide-card reveal reveal-d4">
            <h3>📋 Tamper-Evident Audit</h3>
            <p>Every command, sensor reading, and safety event is logged with cryptographic hashes. Full audit trail for
              compliance.</p>
          </div>
          <div class="guide-card reveal reveal-d5">
            <h3>📡 Sensor Monitoring</h3>
            <p>Continuous monitoring of IMU, current draw, temperature, and proximity. Automatic shutdown on anomalous
              readings.</p>
          </div>
        </div>

        <div class="wizard-after reveal" style="margin-top:40px">
          <h3>Safety CLI commands</h3>
          <div class="code-block">
            <div class="code-header">
              <div class="code-dots"><span></span><span></span><span></span></div><span
                class="code-filename">Terminal</span>
            </div>
            <div class="code-body"><span class="cm"># View active safety rules</span>
              <span class="k">castor safety rules</span>

              <span class="cm"># Verify audit log integrity</span>
              <span class="k">castor audit --verify</span>

              <span class="cm"># Live safety monitor</span>
              <span class="k">castor monitor --watch</span>
            </div>
          </div>
        </div>

        <!-- Protocol 66 -->
        <div class="doc-section reveal" style="margin-top:56px">
          <div class="section-header" style="text-align:center;margin-bottom:32px">
            <p class="section-label">Protocol 66</p>
            <h2 class="section-title" style="font-size:1.75rem">Protocol 66 — Safety Conformance</h2>
            <p class="section-desc" style="margin-left:auto;margin-right:auto">OpenCastor implements Protocol 66 (P66),
              the RCAN safety conformance framework. P66 defines five invariants that must hold at all times — enforced
              at the runtime layer, not the AI layer. See <a href="https://rcan.dev/docs/safety"
              style="color:var(--accent)">rcan.dev/docs/safety</a> for the full specification.</p>
          </div>

          <h3>What is Protocol 66?</h3>
          <p>Protocol 66 is a set of verifiable safety invariants baked into the OpenCastor runtime. Named after RCAN
            §16.6, P66 makes safety properties machine-checkable — any conforming robot runtime can prove it upholds
            all five invariants via the <code>/api/safety/manifest</code> endpoint. This enables auditors, operators,
            and orchestrators to verify safety conformance programmatically without reading source code.</p>

          <h3 style="margin-top:32px">The 5 Invariants</h3>
          <div class="guide-grid" style="margin-top:16px">
            <div class="guide-card reveal">
              <h3>① local_safety_always_wins</h3>
              <p>Local safety checks are evaluated before any remote RCAN command is dispatched. A remote command
                cannot bypass e-stop, bounds, or confidence gates — even if the orchestrator instructs it to.</p>
            </div>
            <div class="guide-card reveal reveal-d1">
              <h3>② safety_messages_bypass_queues</h3>
              <p>RCAN MessageType 6 (safety messages — ESTOP, STOP, RESUME) bypass all queues and confidence gates.
                They are processed with the highest priority, before any pending command in the queue.</p>
            </div>
            <div class="guide-card reveal reveal-d2">
              <h3>③ estop_requires_explicit_clear</h3>
              <p>Once an ESTOP is triggered, it cannot be cleared automatically. It requires an explicit hardware-level
                acknowledgment — preventing AI retry loops from spontaneously resuming a halted robot.</p>
            </div>
            <div class="guide-card reveal reveal-d3">
              <h3>④ ai_cannot_override_safety</h3>
              <p>No AI model output — regardless of provider, confidence, or role — can override a safety constraint.
                The safety layer sits below the AI layer in the call stack and is structurally unreachable by model
                output.</p>
            </div>
            <div class="guide-card reveal reveal-d4">
              <h3>⑤ audit_trail_complete</h3>
              <p>Every safety event, command dispatch, and ESTOP trigger is written to the tamper-evident audit log
                before the action completes. No safety-relevant event can be lost or suppressed.</p>
            </div>
          </div>

          <h3 style="margin-top:40px">GET /api/safety/manifest — Live Conformance Report</h3>
          <p>OpenCastor exposes a real-time conformance report at <code>/api/safety/manifest</code>. Call it from
            any orchestrator, CI pipeline, or monitoring system to verify that all five P66 invariants are
            currently satisfied.</p>

          <div class="code-block reveal" style="margin:16px 0;max-width:720px">
            <div class="code-header">
              <div class="code-dots"><span></span><span></span><span></span></div><span class="code-filename">GET /api/safety/manifest</span>
            </div>
            <div class="code-body"><span class="k">curl</span> http://localhost:8000/api/safety/manifest

              <span class="cm"># Response:</span>
              {'{'}
              <span class="k">"p66_conformant"</span>: <span class="v">true</span>,
              <span class="k">"conformance_pct"</span>: <span class="n">100.0</span>,
              <span class="k">"invariants"</span>: {'{'}
                <span class="k">"local_safety_always_wins"</span>: <span class="v">true</span>,
                <span class="k">"safety_messages_bypass_queues"</span>: <span class="v">true</span>,
                <span class="k">"estop_requires_explicit_clear"</span>: <span class="v">true</span>,
                <span class="k">"ai_cannot_override_safety"</span>: <span class="v">true</span>,
                <span class="k">"audit_trail_complete"</span>: <span class="v">true</span>
              {'}'},
              <span class="k">"watchdog_ok"</span>: <span class="v">true</span>,
              <span class="k">"estop_active"</span>: <span class="v">false</span>,
              <span class="k">"timestamp_ms"</span>: <span class="n">1741996800000</span>
              {'}'}
            </div>
          </div>

          <h3 style="margin-top:40px">Safety Configuration in rcan.yaml</h3>
          <p>The <code>safety</code> block in your RCAN config controls P66 behaviour. The
            <code>local_safety_wins</code> field is required (RCAN §6 invariant) — the wizard generates it by
            default.</p>

          <div class="code-block reveal" style="margin:16px 0;max-width:720px">
            <div class="code-header">
              <div class="code-dots"><span></span><span></span><span></span></div><span class="code-filename">my_robot.rcan.yaml — safety block</span>
            </div>
            <div class="code-body"><span class="k">safety</span>:
              <span class="k">local_safety_wins</span>: <span class="v">true</span>          <span class="cm"># required — RCAN §6</span>
              <span class="k">emergency_stop_distance</span>: <span class="n">15</span>      <span class="cm"># cm — triggers ESTOP</span>
              <span class="k">watchdog</span>:
                <span class="k">enabled</span>: <span class="v">true</span>
                <span class="k">timeout_ms</span>: <span class="n">500</span>               <span class="cm"># heartbeat interval</span>
              <span class="k">confidence_gates</span>:
                <span class="k">CONTROL</span>: <span class="n">0.75</span>                <span class="cm"># min confidence to dispatch control commands</span>
                <span class="k">CONFIG</span>: <span class="n">0.65</span>                 <span class="cm"># min confidence for config changes</span>
                <span class="k">TRAINING</span>: <span class="n">0.60</span>               <span class="cm"># min confidence for training actions</span>
                <span class="k">STATUS</span>: <span class="n">0.0</span>                  <span class="cm"># status queries always allowed</span>
            </div>
          </div>

          <h3 style="margin-top:40px">Hardware Safety Declaration</h3>
          <p>Declare physical safety hardware in the <code>hardware_safety</code> block. This is reflected in
            the conformance manifest and surfaced in the Robot Registry (RRF).</p>

          <div class="code-block reveal" style="margin:16px 0;max-width:720px">
            <div class="code-header">
              <div class="code-dots"><span></span><span></span><span></span></div><span class="code-filename">my_robot.rcan.yaml — hardware_safety block</span>
            </div>
            <div class="code-body"><span class="k">hardware_safety</span>:
              <span class="k">physical_estop</span>: <span class="v">true</span>           <span class="cm"># physical e-stop button present</span>
              <span class="k">sil_level</span>: <span class="n">2</span>                   <span class="cm"># IEC 62061 SIL (1–3)</span>
              <span class="k">ple_level</span>: <span class="s">"d"</span>                <span class="cm"># ISO 13849 PLe (a–e)</span>
              <span class="k">watchdog_hardware</span>: <span class="v">true</span>       <span class="cm"># independent hardware watchdog</span>
              <span class="k">force_torque_sensor</span>: <span class="v">false</span>    <span class="cm"># collision detection via F/T sensor</span>
            </div>
          </div>

          <p style="margin-top:16px">
            Full P66 specification, conformance checker source, and test vectors:
            <a href="https://rcan.dev/docs/safety" style="color:var(--accent)">rcan.dev/docs/safety →</a>
          </p>
        </div>
      </div>
    </section>

    <!-- CLI Reference -->
    <section class="section alt-section" id="cli">
      <div class="container">
        <div class="section-header center reveal">
          <p class="section-label">CLI</p>
          <h2 class="section-title">CLI Reference</h2>
          <p class="section-desc">All <code style="padding:2px 6px;font-size:0.8125rem">castor</code> commands at a
            glance.</p>
        </div>

        <div class="code-block reveal" style="max-width:720px;margin:0 auto">
          <div class="code-header">
            <div class="code-dots"><span></span><span></span><span></span></div><span
              class="code-filename">Terminal</span>
          </div>
          <div class="code-body"><span class="cm"># Core commands</span>
            <span class="k">castor run</span> --config robot.rcan.yaml <span class="cm"># Start the runtime</span>
            <span class="k">castor wizard</span> <span class="cm"># Interactive setup</span>
            <span class="k">castor doctor</span> <span class="cm"># Check deps &amp; hardware</span>

            <span class="cm"># Hub</span>
            <span class="k">castor hub</span> browse <span class="cm"># Browse community recipes</span>
            <span class="k">castor hub</span> install &lt;recipe-id&gt; <span class="cm"># Install a recipe</span>
            <span class="k">castor hub</span> share --config robot.rcan.yaml <span class="cm"># Share your config</span>

            <span class="cm"># Authentication</span>
            <span class="k">castor login</span> <span class="cm"># OAuth login (Anthropic/Google)</span>
            <span class="k">castor login</span> --provider anthropic <span class="cm"># Provider-specific login</span>

            <span class="cm"># Dashboard</span>
            <span class="k">castor dashboard-tui</span> <span class="cm"># Terminal UI dashboard</span>
            <span class="k">castor dashboard</span> <span class="cm"># Web dashboard (CastorDash)</span>

            <span class="cm"># Utilities</span>
            <span class="k">castor safety</span> rules <span class="cm"># View safety rules</span>
            <span class="k">castor audit</span> --verify <span class="cm"># Verify audit log</span>
            <span class="k">castor monitor</span> --watch <span class="cm"># Live sensor monitor</span>
          </div>
        </div>
      </div>
    </section>

    <!-- API Reference -->
    <section class="section" id="api">
      <div class="container">
        <div class="section-header center reveal">
          <p class="section-label">API</p>
          <h2 class="section-title">REST API Reference</h2>
          <p class="section-desc">The OpenCastor runtime exposes a REST API on the configured RCAN protocol port
            (default 8000).</p>
        </div>

        <div style="max-width:720px;margin:0 auto;overflow-x:auto" class="reveal">
          <table class="endpoint-table">
            <thead>
              <tr>
                <th>Method</th>
                <th>Endpoint</th>
                <th>Description</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td class="method">GET</td>
                <td>/api/status</td>
                <td>Robot status, battery, uptime</td>
              </tr>
              <tr>
                <td class="method">GET</td>
                <td>/api/capabilities</td>
                <td>Declared RCAN capabilities</td>
              </tr>
              <tr>
                <td class="method">GET</td>
                <td>/setup/api/catalog</td>
                <td>Shared setup-v2 catalog (providers, stacks, profiles, presets)</td>
              </tr>
              <tr>
                <td class="method-post method">POST</td>
                <td>/setup/api/preflight</td>
                <td>Run setup preflight checks and remediation hints</td>
              </tr>
              <tr>
                <td class="method-post method">POST</td>
                <td>/setup/api/session/start</td>
                <td>Start resumable setup session</td>
              </tr>
              <tr>
                <td class="method">GET</td>
                <td>/setup/api/session/{'{'}session_id{'}'}</td>
                <td>Read setup session state</td>
              </tr>
              <tr>
                <td class="method-post method">POST</td>
                <td>/setup/api/session/{'{'}session_id{'}'}/select</td>
                <td>Persist stage selections for resume</td>
              </tr>
              <tr>
                <td class="method-post method">POST</td>
                <td>/setup/api/remediate</td>
                <td>Execute guided remediation action (consent-gated)</td>
              </tr>
              <tr>
                <td class="method-post method">POST</td>
                <td>/setup/api/verify-config</td>
                <td>Pre-save dry-run verification gate</td>
              </tr>
              <tr>
                <td class="method-post method">POST</td>
                <td>/setup/api/generate-config</td>
                <td>Generate and save RCAN config from setup selections</td>
              </tr>
              <tr>
                <td class="method-post method">POST</td>
                <td>/setup/api/test-provider</td>
                <td>Compatibility health check for provider credentials/readiness</td>
              </tr>
              <tr>
                <td class="method">GET</td>
                <td>/setup/api/metrics</td>
                <td>Local setup reliability metrics</td>
              </tr>
              <tr>
                <td class="method-post method">POST</td>
                <td>/api/command</td>
                <td>Send an RCAN command</td>
              </tr>
              <tr>
                <td class="method-post method">POST</td>
                <td>/api/nav/goto</td>
                <td>Navigate to coordinates</td>
              </tr>
              <tr>
                <td class="method-post method">POST</td>
                <td>/api/teleop</td>
                <td>Manual teleoperation input</td>
              </tr>
              <tr>
                <td class="method">GET</td>
                <td>/api/vision/snapshot</td>
                <td>Current camera frame + detections</td>
              </tr>
              <tr>
                <td class="method">GET</td>
                <td>/api/vision/depth</td>
                <td>Depth map (OAK-D only)</td>
              </tr>
              <tr>
                <td class="method-post method">POST</td>
                <td>/api/chat</td>
                <td>Send chat message to agent</td>
              </tr>
              <tr>
                <td class="method">GET</td>
                <td>/api/safety/rules</td>
                <td>Active safety rules</td>
              </tr>
              <tr>
                <td class="method">GET</td>
                <td>/api/audit/log</td>
                <td>Audit log entries</td>
              </tr>
              <tr>
                <td class="method-post method">POST</td>
                <td>/api/estop</td>
                <td>Trigger emergency stop</td>
              </tr>
              <tr>
                <td class="method-del method">DELETE</td>
                <td>/api/estop</td>
                <td>Release emergency stop</td>
              </tr>
              <tr>
                <td class="method">GET</td>
                <td>/ws/stream</td>
                <td>WebSocket: real-time sensor data</td>
              </tr>
            </tbody>
          </table>
        </div>

        <div class="code-block reveal" style="max-width:720px;margin:24px auto 0">
          <div class="code-header">
            <div class="code-dots"><span></span><span></span><span></span></div><span class="code-filename">Example —
              Send a command</span>
          </div>
          <div class="code-body"><span class="k">curl</span> -X POST http://localhost:8000/api/command \
            -H <span class="s">"Content-Type: application/json"</span> \
            -d <span class="s">'{'{'}"action": "nav.goto", "params": {'{'}"x": 1.0, "y": 2.5{'}'}{'}'}'</span></div>
        </div>
      </div>
    </section>

    <!-- §19 Skill Invocation -->
    <section class="section" id="skill-invocation">
      <div class="container">
        <div class="section-header center reveal">
          <p class="section-label">§19 · RCAN v1.4</p>
          <h2 class="section-title">Skill Invocation</h2>
          <p class="section-desc">OpenCastor supports RCAN §19 Skill Invocation — structured remote calls
            from an orchestrator to a robot's registered skills via <code>INVOKE</code>,
            <code>INVOKE_RESULT</code>, and <code>INVOKE_CANCEL</code> message types.</p>
        </div>

        <div style="max-width:720px;margin:0 auto;overflow-x:auto" class="reveal">
          <table class="endpoint-table">
            <thead>
              <tr>
                <th>Method</th>
                <th>Endpoint</th>
                <th>Description</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td class="method">GET</td>
                <td>/api/skills</td>
                <td>List registered skills and their schemas</td>
              </tr>
              <tr>
                <td class="method-post method">POST</td>
                <td>/api/skills/invoke</td>
                <td>Invoke a skill (emits RCAN INVOKE; returns INVOKE_RESULT)</td>
              </tr>
              <tr>
                <td class="method-post method">POST</td>
                <td>/api/skills/invoke/{'{'}invocation_id{'}'}/cancel</td>
                <td>Cancel an in-progress skill invocation (INVOKE_CANCEL)</td>
              </tr>
              <tr>
                <td class="method">GET</td>
                <td>/api/skills/invoke/{'{'}invocation_id{'}'}</td>
                <td>Poll invocation status and result</td>
              </tr>
            </tbody>
          </table>
        </div>

        <div class="code-block reveal" style="max-width:720px;margin:24px auto 0">
          <div class="code-header">
            <div class="code-dots"><span></span><span></span><span></span></div><span class="code-filename">Example — Invoke a skill</span>
          </div>
          <div class="code-body"><span class="k">curl</span> -X POST http://localhost:8000/api/skills/invoke \
            -H <span class="s">"Content-Type: application/json"</span> \
            -d <span class="s">'{'{'}"skill": "navigate_to", "params": {'{'}"x": 1.0, "y": 2.5{'}'}, "timeout_ms": 5000{'}'}'</span></div>
        </div>

        <p class="reveal" style="max-width:720px;margin:16px auto 0;color:var(--text-secondary);font-size:0.9rem">
          See <a href="https://rcan.dev/spec/section-19/" style="color:var(--accent)">RCAN §19 Skill Invocation spec</a> for
          full message-type definitions and flow diagrams.
        </p>
      </div>
    </section>

    <!-- Docker -->
    <section class="section alt-section" id="docker">
      <div class="container">
        <div class="section-header reveal" style="text-align:center;margin-left:auto;margin-right:auto">
          <p class="section-label">Docker</p>
          <h2 class="section-title">Run in a container</h2>
          <p class="section-desc" style="margin-left:auto;margin-right:auto">Deploy OpenCastor with Docker for
            reproducible builds and production deployments.</p>
        </div>

        <div class="code-block reveal" style="max-width:720px;margin:0 auto">
          <div class="code-header">
            <div class="code-dots"><span></span><span></span><span></span></div><span
              class="code-filename">Terminal</span>
          </div>
          <div class="code-body"><span class="cm"># Clone and start</span>
            <span class="k">git clone</span> https://github.com/craigm26/OpenCastor.git
            <span class="k">cd</span> OpenCastor
            <span class="k">docker compose up -d</span>

            <span class="cm"># Custom config</span>
            <span class="k">docker compose run</span> <span class="s">-v
              ./my_robot.rcan.yaml:/app/config.rcan.yaml</span> \
            <span class="s">--env-file .env</span> \
            castor run --config /app/config.rcan.yaml
          </div>
        </div>
      </div>
    </section>

    <!-- Self-Improving Loop -->
    <section class="section alt-section" id="self-improving">
      <div class="container">
        <div class="section-header center reveal">
          <p class="section-label">Intelligence</p>
          <h2 class="section-title">Self-Improving Loop</h2>
          <p class="section-desc">Your robot learns from its mistakes. The Sisyphus Loop analyzes episodes, identifies
            failures, generates fixes, and applies improvements — automatically.</p>
        </div>
        <div class="doc-content reveal">
          <pre><code># Enable from CLI (disabled by default)
castor improve --enable
castor improve --enable --config mybot.rcan.yaml

# Disable anytime
castor improve --disable

# Run analysis manually
castor improve --episodes 10

# Check status and history
castor improve --status
castor improve --improvements
castor improve --rollback abc123

# ALMA batch consolidation (cross-episode patterns)
castor improve --batch --config bot.rcan.yaml</code></pre>

          <p style="margin-top:1.5rem"><strong>How it works:</strong></p>
          <ul style="margin-top:0.5rem;line-height:1.8">
            <li><strong>PM Stage</strong> — Analyzes episode outcomes, identifies failures and root causes</li>
            <li><strong>Dev Stage</strong> — Generates config/behavior patches from analysis</li>
            <li><strong>QA Stage</strong> — Verifies patches are safe (bounds checking, regression tests)</li>
            <li><strong>Apply Stage</strong> — Deploys verified patches (rollback available)</li>
          </ul>

          <p style="margin-top:1.5rem"><strong>Cost-aware presets (set in wizard or YAML):</strong></p>
          <ul style="margin-top:0.5rem;line-height:1.8">
            <li><strong>Free</strong> — Ollama/llama.cpp local analysis ($0)</li>
            <li><strong>Budget</strong> — HuggingFace free API ($0/month)</li>
            <li><strong>Smart</strong> — Gemini 2.5 Flash-Lite (~$1-3/month)</li>
            <li><strong>Premium</strong> — Claude for best root-cause analysis (~$5-15/month)</li>
          </ul>

          <p style="margin-top:1.5rem">Config and behavior patches can auto-apply within safe bounds. Code patches
            always require human review. Enable via <code>castor wizard</code> or directly in your RCAN config:</p>

          <pre style="margin-top:0.75rem"><code>learner:
  enabled: true
  provider: huggingface
  model: Qwen/Qwen2.5-7B-Instruct
  cadence_n: 5
  auto_apply_config: true
  auto_apply_behavior: false
  auto_apply_code: false</code></pre>
        </div>
      </div>
    </section>

    <!-- Agent Swarm -->
    <section class="section" id="agent-swarm">
      <div class="container">
        <div class="section-header center reveal">
          <p class="section-label">Intelligence</p>
          <h2 class="section-title">Agent Swarm Architecture</h2>
          <p class="section-desc">Multiple specialized AI agents running concurrently — each an expert in its domain.
          </p>
        </div>
        <div class="doc-content reveal">
          <pre><code># Phase 2: Observer + Navigator
from castor.agents import ObserverAgent, NavigatorAgent, SharedState, AgentRegistry

state = SharedState()
reg = AgentRegistry()
reg.register(ObserverAgent)
reg.register(NavigatorAgent)

observer = reg.spawn("observer", config={'{'}"obstacle_labels": ["bottle"]{'}'})
navigator = reg.spawn("navigator", config={'{'}"max_speed": 0.5{'}'})

# CLI
castor agents list
castor agents spawn --name observer
castor agents status</code></pre>

          <pre style="margin-top:1rem"><code># Phase 3: Task Specialists
from castor.specialists import TaskPlanner, ManipulatorSpecialist, ScoutSpecialist
from castor.specialists import Task

planner = TaskPlanner([ManipulatorSpecialist(), ScoutSpecialist()])
task = Task(id="t1", type="grasp", goal="Grasp the red block",
            params={'{'}"object_position": [0.5, 0.3, 0.1]{'}'}, priority=3, created_at=time.time(), deadline_s=None)
planner.submit(task)
result = await planner.run_next()</code></pre>

          <pre style="margin-top:1rem"><code># Phase 4: Swarm Coordination
from castor.swarm import SwarmCoordinator, SharedMemory, SwarmConsensus

mem = SharedMemory(robot_id="bob-001")
consensus = SwarmConsensus(robot_id="bob-001", shared_memory=mem)
coordinator = SwarmCoordinator(my_robot_id="bob-001", shared_memory=mem, consensus=consensus)

coordinator.add_peer(peer)  # discovered via mDNS
assignment = coordinator.assign_next()
print(coordinator.fleet_status())</code></pre>
        </div>
      </div>
    </section>

    <!-- Troubleshooting -->
    <section class="section" id="troubleshooting">
      <div class="container">
        <div class="section-header center reveal">
          <p class="section-label">Help</p>
          <h2 class="section-title">Troubleshooting</h2>
        </div>

        <div style="max-width:720px;margin:0 auto">
          <div class="faq-item reveal">
            <div class="faq-q">castor doctor shows "Hailo not found"</div>
            <div class="faq-a">Install the Hailo runtime with <code
                style="padding:2px 5px;font-size:0.8125rem">sudo apt install hailo-all</code> and reboot. Ensure your Pi
              5 has the AI Kit M.2 module properly seated.</div>
          </div>
          <div class="faq-item reveal">
            <div class="faq-q">OAK-D camera not detected</div>
            <div class="faq-a">Run <code style="padding:2px 5px;font-size:0.8125rem">pip install depthai</code> to
              install DepthAI v3. Check USB3 connection — OAK-D requires USB3 for depth. Try <code
                style="padding:2px 5px;font-size:0.8125rem">python -c "import depthai; print(depthai.Device.getAllAvailableDevices())"</code>
            </div>
          </div>
          <div class="faq-item reveal">
            <div class="faq-q">Apple provider says Apple Intelligence is not enabled</div>
            <div class="faq-a">Enable Apple Intelligence in macOS System Settings, keep the device online so model assets
              can download, then re-run <code style="padding:2px 5px;font-size:0.8125rem">castor wizard</code> or
              <code style="padding:2px 5px;font-size:0.8125rem">POST /setup/api/preflight</code>.</div>
          </div>
          <div class="faq-item reveal">
            <div class="faq-q">Apple preflight reports device not eligible or model not ready</div>
            <div class="faq-a">Use guided fallback to <code style="padding:2px 5px;font-size:0.8125rem">mlx_local_vision</code>
              (Apple Silicon) or <code style="padding:2px 5px;font-size:0.8125rem">ollama_universal_local</code>
              (cross-platform), then retry Apple later.</div>
          </div>
          <div class="faq-item reveal">
            <div class="faq-q">Apple SDK import fails during setup</div>
            <div class="faq-a">Accept setup's one-time install prompt, or install manually with <code
                style="padding:2px 5px;font-size:0.8125rem">pip install "git+https://github.com/apple/python-apple-fm-sdk.git@3204b7ee892131a5d2c940d95caaabc90b4a40c9"</code>.
              Ensure Xcode 26+ is installed and <code style="padding:2px 5px;font-size:0.8125rem">xcodebuild</code> is
              available.</div>
          </div>
          <div class="faq-item reveal">
            <div class="faq-q">Provider authentication fails</div>
            <div class="faq-a">Run <code
                style="padding:2px 5px;font-size:0.8125rem">castor login --provider &lt;name&gt;</code> to
              re-authenticate. Check that your <code style="padding:2px 5px;font-size:0.8125rem">.env</code> file has
              the correct key name (ANTHROPIC_API_KEY, GOOGLE_API_KEY, etc.).</div>
          </div>
          <div class="faq-item reveal">
            <div class="faq-q">PCA9685 I2C not responding</div>
            <div class="faq-a">Verify I2C is enabled: <code
                style="padding:2px 5px;font-size:0.8125rem">sudo raspi-config</code> → Interface Options → I2C. Check
              wiring and run <code style="padding:2px 5px;font-size:0.8125rem">i2cdetect -y 1</code> to confirm address
              0x40.</div>
          </div>
          <div class="faq-item reveal">
            <div class="faq-q">Robot moves erratically or too fast</div>
            <div class="faq-a">Check your <code style="padding:2px 5px;font-size:0.8125rem">reactive.max_velocity</code>
              setting. Default is 1.5 m/s. Lower it for indoor use. Also verify your motor calibration in the drivers
              block.</div>
          </div>
          <div class="faq-item reveal">
            <div class="faq-q">WhatsApp QR code not scanning</div>
            <div class="faq-a">Ensure your Pi has internet access. The WhatsApp bridge needs to download session data on
              first connect. Try <code style="padding:2px 5px;font-size:0.8125rem">castor wizard</code> again and select
              the WhatsApp channel option.</div>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA -->
    <section class="cta-section">
      <h2 class="reveal">Ready to build?</h2>
      <p class="reveal reveal-d1">Join the community and start building intelligent robots today.</p>
      <div class="hero-actions reveal reveal-d2">
        <a href="https://github.com/craigm26/OpenCastor" class="btn btn-primary btn-lg">
          <svg viewBox="0 0 24 24" fill="currentColor" width="18" height="18">
            <path
              d="M12 2C6.477 2 2 6.477 2 12c0 4.42 2.865 8.17 6.839 9.49.5.09.682-.217.682-.482 0-.237-.008-.866-.013-1.7-2.782.604-3.369-1.34-3.369-1.34-.454-1.156-1.11-1.464-1.11-1.464-.908-.62.069-.608.069-.608 1.003.07 1.531 1.03 1.531 1.03.892 1.529 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.11-4.555-4.943 0-1.091.39-1.984 1.029-2.683-.103-.253-.446-1.27.098-2.647 0 0 .84-.269 2.75 1.025A9.578 9.578 0 0112 6.836a9.59 9.59 0 012.504.337c1.909-1.294 2.747-1.025 2.747-1.025.546 1.377.203 2.394.1 2.647.64.699 1.028 1.592 1.028 2.683 0 3.842-2.339 4.687-4.566 4.935.359.309.678.919.678 1.852 0 1.336-.012 2.415-.012 2.743 0 .267.18.578.688.48C19.138 20.167 22 16.418 22 12c0-5.523-4.477-10-10-10z" />
          </svg>
          View on GitHub
        </a>
        <a href="https://discord.gg/jMjA8B26Bq" class="btn btn-secondary btn-lg">Join our Discord</a>
      </div>
    </section>
  </main>

  <footer class="site-footer">
    <div class="footer-inner">
      <div class="footer-grid">
        <div class="footer-brand">
          <a href="/" class="nav-logo">
      <div class="nav-logo-img"></div>
      <span>OpenCastor</span>
    </a>
          <p>The universal runtime for embodied AI.</p>
        </div>
        <div class="footer-col">
          <h4>Product</h4><a href="/docs">Documentation</a><a href="/hardware">Hardware</a><a
            href="https://rcan.dev/spec/">RCAN Spec</a><a href="https://pypi.org/project/opencastor/">PyPI</a>
        </div>
        <div class="footer-col">
          <h4>Resources</h4><a href="/docs">Getting Started</a><a
            href="https://github.com/craigm26/OpenCastor">GitHub</a><a
            href="https://github.com/craigm26/OpenCastor/issues">Issues</a><a
            href="https://discord.gg/jMjA8B26Bq">Discord</a>
        </div>
        <div class="footer-col">
          <h4>Company</h4><a href="/hub">Hub</a><a href="/beginners">Beginners</a>
          <a href="/about">About</a><a href="mailto:hello@opencastor.com">Contact</a>
        </div>
      </div>
      <div class="footer-bottom">
        <p>&copy; 2025-2026 Craig Merry. Apache 2.0 License.</p>
        <div class="footer-social">
          <a href="https://github.com/craigm26" aria-label="GitHub"><svg viewBox="0 0 24 24" fill="currentColor">
              <path
                d="M12 2C6.477 2 2 6.477 2 12c0 4.42 2.865 8.17 6.839 9.49.5.09.682-.217.682-.482 0-.237-.008-.866-.013-1.7-2.782.604-3.369-1.34-3.369-1.34-.454-1.156-1.11-1.464-1.11-1.464-.908-.62.069-.608.069-.608 1.003.07 1.531 1.03 1.531 1.03.892 1.529 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.11-4.555-4.943 0-1.091.39-1.984 1.029-2.683-.103-.253-.446-1.27.098-2.647 0 0 .84-.269 2.75 1.025A9.578 9.578 0 0112 6.836a9.59 9.59 0 012.504.337c1.909-1.294 2.747-1.025 2.747-1.025.546 1.377.203 2.394.1 2.647.64.699 1.028 1.592 1.028 2.683 0 3.842-2.339 4.687-4.566 4.935.359.309.678.919.678 1.852 0 1.336-.012 2.415-.012 2.743 0 .267.18.578.688.48C19.138 20.167 22 16.418 22 12c0-5.523-4.477-10-10-10z" />
            </svg></a>
          <a href="https://twitter.com/opencastor" aria-label="Twitter / X"><svg viewBox="0 0 24 24"
              fill="currentColor">
              <path
                d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z" />
            </svg></a>
          <a href="https://discord.gg/jMjA8B26Bq" aria-label="Discord"><svg viewBox="0 0 24 24" fill="currentColor">
              <path
                d="M20.317 4.37a19.791 19.791 0 00-4.885-1.515.074.074 0 00-.079.037c-.21.375-.444.864-.608 1.25a18.27 18.27 0 00-5.487 0 12.64 12.64 0 00-.617-1.25.077.077 0 00-.079-.037A19.736 19.736 0 003.677 4.37a.07.07 0 00-.032.027C.533 9.046-.32 13.58.099 18.057a.082.082 0 00.031.057 19.9 19.9 0 005.993 3.03.078.078 0 00.084-.028c.462-.63.874-1.295 1.226-1.994a.076.076 0 00-.041-.106 13.107 13.107 0 01-1.872-.892.077.077 0 01-.008-.128 10.2 10.2 0 00.372-.292.074.074 0 01.077-.01c3.928 1.793 8.18 1.793 12.062 0a.074.074 0 01.078.01c.12.098.246.198.373.292a.077.077 0 01-.006.127 12.299 12.299 0 01-1.873.892.077.077 0 00-.041.107c.36.698.772 1.362 1.225 1.993a.076.076 0 00.084.028 19.839 19.839 0 006.002-3.03.077.077 0 00.032-.054c.5-5.177-.838-9.674-3.549-13.66a.061.061 0 00-.031-.03zM8.02 15.33c-1.183 0-2.157-1.085-2.157-2.419 0-1.333.956-2.419 2.157-2.419 1.21 0 2.176 1.095 2.157 2.42 0 1.333-.956 2.418-2.157 2.418zm7.975 0c-1.183 0-2.157-1.085-2.157-2.419 0-1.333.956-2.419 2.157-2.419 1.21 0 2.176 1.095 2.157 2.42 0 1.333-.947 2.418-2.157 2.418z" />
            </svg></a>
        </div>
      </div>
    </div>
  </footer>

  
<script>
  // Theme pill — clicking ☀ or 🌙 sets that theme directly; clicking pill body toggles
  (function() {
    var t = localStorage.getItem('oc-theme');
    var dark = window.matchMedia('(prefers-color-scheme: dark)').matches;
    document.documentElement.setAttribute('data-theme', t || (dark ? 'dark' : 'light'));
  })();

  document.addEventListener('DOMContentLoaded', function() {
    function applyTheme(theme) {
      document.documentElement.setAttribute('data-theme', theme);
      localStorage.setItem('oc-theme', theme);
    }
    document.querySelectorAll('.theme-pill').forEach(function(pill) {
      pill.addEventListener('click', function(e) {
        var opt = e.target.closest('[data-opt]');
        if (opt) {
          applyTheme(opt.dataset.opt);
        } else {
          var cur = document.documentElement.getAttribute('data-theme') || 'dark';
          applyTheme(cur === 'dark' ? 'light' : 'dark');
        }
      });
    });
  });
</script>

</BaseLayout>
